import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Move turbopack out of experimental
  turbopack: {}, 

  // Any other standard config options go here (e.g., images, redirects)
};

export default nextConfig;